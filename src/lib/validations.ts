import { z } from "zod";const required="Preencha este campo para continuar.";const validHours=["08:30","09:00","10:00","11:00","13:00","14:00","15:00","16:00","17:00"];export const bookingSchema=z.object({nome:z.string().trim().min(2,required),whatsapp:z.string().trim().min(10,"Informe um WhatsApp válido."),pet:z.string().trim().min(1,required),tipoPet:z.string().min(1,required),servico:z.string().min(1,required),data:z.string().min(1,required),horario:z.string().min(1,required),observacoes:z.string().max(500,"Use no máximo 500 caracteres.").optional()}).superRefine((v,ctx)=>{const date=new Date(`${v.data}T12:00:00`);if(Number.isNaN(date.getTime())){ctx.addIssue({code:"custom",path:["data"],message:required});return}const today=new Date();today.setHours(0,0,0,0);const selected=new Date(date);selected.setHours(0,0,0,0);if(selected<today)ctx.addIssue({code:"custom",path:["data"],message:"Escolha uma data futura ou de hoje."});const day=date.getDay();if(day===0)ctx.addIssue({code:"custom",path:["data"],message:"Não atendemos aos domingos."});if(!validHours.includes(v.horario))ctx.addIssue({code:"custom",path:["horario"],message:"Escolha um horário disponível."});if(day===6&&! ["08:30","09:00","10:00","11:00"].includes(v.horario))ctx.addIssue({code:"custom",path:["horario"],message:"Aos sábados atendemos somente até 13:00."});});export type BookingFormData=z.infer<typeof bookingSchema>;export const availableHours=validHours;
