"use client";import Image from "next/image";import Link from "next/link";import { Menu, X, MessageCircle, ChevronRight } from "lucide-react";import { useState } from "react";import { navigation } from "@/constants/navigation";import { defaultWhatsAppUrl } from "@/lib/whatsapp";export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-trevo-dark/5 bg-[#FAF7EF]/88 backdrop-blur-2xl"><div className="container-trevo flex h-[140px] items-center justify-between"><Link href="#inicio" aria-label="Ir para o início"><Image src="/images/logotipo-trevo-veterinaria.png" alt="Veterinária Trevo" width={182} height={72} className="h-[140px] w-auto object-contain" priority/></Link><nav className="hidden items-center gap-7 lg:flex" aria-label="Menu principal">{navigation.map((item,i)=><a key={item.href} href={item.href} className={`relative text-[13px] font-black text-trevo-dark transition hover:text-trevo-soft ${i===0?"after:absolute after:-bottom-2 after:left-0 after:h-[2px] after:w-full after:rounded-full after:bg-trevo-primary":""}`}>{item.label}</a>)}</nav><a href={defaultWhatsAppUrl} className="hidden items-center gap-2 rounded-full bg-trevo-soft px-5 py-3 text-[13px] font-black text-white shadow-button transition hover:scale-105 hover:bg-trevo-primary md:inline-flex"><MessageCircle size={17}/>Agendar pelo WhatsApp</a><button onClick={()=>setOpen(true)} className="grid h-11 w-11 place-items-center rounded-full border border-trevo-dark/10 bg-white text-trevo-dark lg:hidden" aria-label="Abrir menu"><Menu/></button></div>{open&&<div className="fixed inset-0 z-[60] lg:hidden" role="dialog" aria-modal="true"><button className="absolute inset-0 bg-trevo-dark/45 backdrop-blur-sm" onClick={()=>setOpen(false)} aria-label="Fechar menu"/><aside className="absolute right-0 top-0 h-dvh w-[88%] max-w-sm rounded-l-[2rem] bg-[#FAF7EF] p-6 shadow-2xl animate-in slide-in-from-right duration-300"><div className="mb-7 flex items-center justify-between"><Image src="/images/logotipo-trevo-veterinaria.png" alt="Veterinária Trevo" width={150} height={60}/><button onClick={()=>setOpen(false)} className="grid h-10 w-10 place-items-center rounded-full bg-white"><X/></button></div><nav className="grid gap-2">{navigation.map(item=><a onClick={()=>setOpen(false)} key={item.href} href={item.href} className="flex items-center justify-between rounded-2xl bg-white px-4 py-4 text-sm font-black text-trevo-dark shadow-sm"><span>{item.label}</span><ChevronRight size={18}/></a>)}</nav><a href={defaultWhatsAppUrl} className="mt-6 flex items-center justify-center gap-2 rounded-full bg-trevo-primary px-5 py-4 font-black text-white"><MessageCircle/>Agendar pelo WhatsApp</a><p className="mt-5 text-center text-sm text-trevo-muted">Atendimento acolhedor para cães e gatos em Rio Grande da Serra.</p></aside></div>}</header>}
