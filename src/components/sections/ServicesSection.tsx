"use client";import { motion } from "framer-motion";import { services } from "@/constants/services";import { SectionTitle } from "@/components/ui/SectionTitle";export function ServicesSection(){return <section id="servicos" className="section-pad relative"><div aria-hidden className="absolute left-0 top-20 h-40 w-40 rounded-full border-[26px] border-trevo-soft/5"/><div className="container-trevo"><SectionTitle center eyebrow="NOSSOS SERVIÇOS" title="Tudo o que seu pet precisa para viver bem"/><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-6">{services.map((s,i)=>{const Icon=s.icon;return <motion.article key={s.title} initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:"-60px"}} transition={{delay:i*.05}} className="card-hover rounded-[1.7rem] border border-trevo-line bg-white p-5 text-center shadow-card"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-trevo-dark text-white shadow-button"><Icon size={28}/></div><h3 className="mt-5 text-lg font-black text-trevo-dark">{s.title}</h3><p className="mt-2 min-h-[72px] text-sm leading-relaxed text-trevo-muted">{s.description}</p></motion.article>})}</div></div></section>}
