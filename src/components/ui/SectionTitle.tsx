export function SectionTitle({eyebrow,title,center=false,subtitle}:{eyebrow:string;title:string;center?:boolean;subtitle?:string}){return <div className={`${center?"mx-auto text-center":""} max-w-3xl`}><p className="mb-3 text-xs font-black tracking-[.24em] text-trevo-soft">{eyebrow}</p><h2 className="text-[1.9rem] font-black leading-[1.05] text-trevo-dark md:text-5xl whitespace-pre-line">{title}</h2>{subtitle&&<p className="mt-4 text-base leading-relaxed text-trevo-muted md:text-lg">{subtitle}</p>}</div>}
